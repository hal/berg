{
  "name": "mocha-js-playground",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "npx tsc --build --clean && tsc --build",
    "pretest": "npm run compile",
    "test": "npm run run-e2e-tests --prefix packages/hal-testsuite",
    "predevelop" : "npm run compile",
    "develop" : "npm run develop --prefix packages/hal-testsuite"
  },
  "workspaces": [
    "./packages/*"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "axios": "^1.1.3",
    "testcontainers": "^8.16.0",
    "typescript": "^4.8.4"
  }
}
